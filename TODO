- not to read whole corpus: done. TODO: read the file again, until there is no batch which gives any improvement. For this, there should be another validation set?
- save HS y into log|V| sized vector instead of |V|, store only the decision nodes with +- values
- save huffman tree into file and change the prediction according to it
  - create a predictor that takes a language model, accepts word ngrams from stdin, and gives words as next word
- create ppl cost into HierarchicalSoftmax
